<UserControl x:Class="PreventSleep.Settings.PreventSleepSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <ScrollViewer Margin="20">
        <StackPanel>
            <CheckBox IsChecked="{Binding Settings.ShowSwitchModeItemOnTopPanel}"
                      Content="{DynamicResource LOC_PreventSleep_ShowSwitchModeItemOnTopPanel}"/>
            <CheckBox Margin="0,10,0,0" IsChecked="{Binding Settings.AlwaysPreventSleepWhenPlayingGames}"
                      Content="{DynamicResource LOC_PreventSleep_AlwaysPreventSleepWhenPlayingGames}"/>
        </StackPanel>
    </ScrollViewer>
</UserControl>