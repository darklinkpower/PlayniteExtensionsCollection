<UserControl x:Class="OpenCriticMetadata.OpenCriticMetadataSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="600">
    <ScrollViewer Margin="20">
        <StackPanel>
            <DockPanel>
                <TextBlock Text="OpenCritic API Key:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <TextBox Text="{Binding Settings.ApiKey, UpdateSourceTrigger=PropertyChanged}" />
            </DockPanel>
        </StackPanel>
    </ScrollViewer>
</UserControl>