<UserControl x:Class="GamePassCatalogBrowser.Views.CatalogBrowserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:GamePassCatalogBrowser"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="1000">
    <Grid>
        <Grid.Resources>
            <Style TargetType="TextBlock" BasedOn="{StaticResource BaseTextBlockStyle}" />
        </Grid.Resources>
        <Grid Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="700" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <DockPanel Grid.Row="0">
                    <TextBlock Text="Collection:" Margin="0,0,0,0" VerticalAlignment="Center"/>
                    <ComboBox Name="ComboBoxCollections" ItemsSource="{Binding Collections}" IsSynchronizedWithCurrentItem="True"
                          Margin="10,0,0,0"/>
                </DockPanel>

                <ListBox Grid.Row="1" Name="ListBoxImages" Margin="0,10,0,0" ItemsSource="{Binding GamePassGames}" IsSynchronizedWithCurrentItem="True"
                    ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                    BorderThickness="0"
                    ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="4" Background="#33000000">
                                <DockPanel>
                                    <TextBlock DockPanel.Dock="Bottom" Text="{Binding GameName}"
                                       Margin="0,3,0,0" Width="150"
                                       HorizontalAlignment="Left" VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                                    <Image Width="150" Height="225"
                                       Source="{Binding CoverLowRes, IsAsync=True}"
                                       DockPanel.Dock="Top"
                                       Stretch="Uniform" StretchDirection="Both" HorizontalAlignment="Center"/>
                                </DockPanel>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Grid>
            <StackPanel Grid.Column="1" Margin="10,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" MinHeight="40"/>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Column="0" FontSize="{DynamicResource FontSizeLargest}" TextWrapping="Wrap" Text="{Binding ElementName=ListBoxImages, Path=SelectedItem.GameName}" VerticalAlignment="Center"/>
                    <Grid Grid.Row="1" Margin="0,10,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Label Grid.Row="0" Grid.Column="0" Content="Developer:"/>
                        <Label Grid.Row="0" Grid.Column="1" Margin="10,0,0,0"
                                       Content="{Binding ElementName=ListBoxImages, Path=SelectedItem.DeveloperName}"/>
                        <Label Grid.Row="1" Grid.Column="0" Content="Publisher:"/>
                        <Label Grid.Row="1" Grid.Column="1" Margin="10,0,0,0"
                                       Content="{Binding ElementName=ListBoxImages, Path=SelectedItem.PublisherName}"/>
                    </Grid>
                    <StackPanel Orientation="Horizontal" Grid.Row="2" Margin="0,10,0,0">
                        <Button Content="View in Microsoft Store" Command="{Binding StoreViewCommand}" DockPanel.Dock="Left" Padding="5"/>
                    </StackPanel>
                    <ScrollViewer Grid.Row="3" Margin="0,10,0,0" VerticalScrollBarVisibility="Auto">
                        <TextBlock TextTrimming="CharacterEllipsis" TextWrapping="Wrap"
                               Text="{Binding ElementName=ListBoxImages, Path=SelectedItem.ProductDescription}"/>
                    </ScrollViewer>
                </Grid>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
